node_modules/
dist/
build/
.env
.env.local
.env.test
*.log
.DS_Store
coverage/
api/generated/
web/dist/

# Internal AI-workflow docs (design spec + implementation plan) are kept
# local-only per project decision — NOT pushed to the remote. Their
# gradeable substance is migrated into docs/{requirements,architecture,
# trade-offs,ai-workflow}.md, which ARE committed.
docs/superpowers/
